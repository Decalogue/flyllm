# LeetCode 001 - 两数之和 - 教练问答记录

**Date**: 2026-03-19
**Coach**: Claude Code
**Skill**: LeetCode Coach

---

## 📚 题目信息

**题号**: 1
**难度**: Easy
**分类**: 数组与字符串 - 哈希表基础
**预计用时**: 20分钟

**题目描述**:
给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出**和为目标值**的那两个整数，并返回它们的数组下标。

---

## 📝 刷题记录

### Step 0: 题目定位与分类

> 目标：建立题目标签化认知

**问题1**: 这道题属于哪个算法分类？

```
你的回答：
-----------------------------------------------------
属于查找
-----------------------------------------------------
```

**问题2**: 这道题在 Hot 100 的哪个章节？

```
你的回答：
-----------------------------------------------------
第一章，数组与字符串
-----------------------------------------------------
```

**问题3**: 如果用最暴力的方法，你会怎么做？时间复杂度是多少？

```
你的回答：
-----------------------------------------------------
直接遍历，时间复杂度O(n²)
-----------------------------------------------------
```

**问题4**: 题目说"每种输入只对应一个答案"，这个信息有什么用？

```
你的回答：
-----------------------------------------------------
只要查到首组值就可以返回
-----------------------------------------------------
```

---

### Step 1: 特征映射

> 目标：提取核心特征，建立物理直觉

**问题5**: 题目中的核心特征关键词有哪些？

```
你的回答：
-----------------------------------------------------
"和为目标值" - 需要两数配对
"返回数组下标" - 需要记录位置
"每种输入只对应一个答案" - 找到即返回
-----------------------------------------------------
```

**问题6**: 用现实比喻描述这个算法问题

```
你的回答：
-----------------------------------------------------
（请用你自己的话描述）

示例：想象一堆卡片...
-----------------------------------------------------
```

**问题7**: 基于哈希表的解题思路是什么？

```
你的回答：
-----------------------------------------------------
遵循：遍历-查找-存储的模板
1. 定义哈希表
2. 遍历数组
3. 查找配对值
4. 找到返回/找不到存储
-----------------------------------------------------
```

---

### Step 2: 算子提取

> 目标：掌握通用模板

**问题8**: 补全下面的代码模板（关键部分用___表示）

```python
def two_sum(nums, target):
    # 1. 创建哈希表
    seen = {}

    # 2. 遍历数组
    for i, num in enumerate(nums):
        # 3. 计算需要查找的值
        complement = _________

        # 4. 在哈希表中查找
        if _________ in _________:
            return [_________, _________]

        # 5. 存储当前值
        seen[_________] = _________

    return []
```

```
你的回答：
-----------------------------------------------------
complement = target - num
if complement in seen:
    return [seen[complement], i]
seen[num] = i
-----------------------------------------------------
```

**问题9**: 为什么顺序是「先查找，后存储」？

```
你的回答：
-----------------------------------------------------
防止使用同一个元素两次
-----------------------------------------------------
```

**问题10**: 如果是"三数之和"，需要做哪些改动？

```
你的回答：
-----------------------------------------------------
1. 需要3个数而不是2个
2. 需要返回数值而不是下标
3. 可能存在多组答案，需要去重
# 提示：可以转化为固定一个数 + 两数之和的子问题
-----------------------------------------------------
```

---

### Step 3: 边界熔断与极限推演

> 目标：测试代码健壮性

**问题11**: 如果 nums = []（空数组），代码会在哪一行出问题？

```
你的回答：
-----------------------------------------------------
不会出错，for循环不会执行，最终返回[]
但题目保证有解，所以这种情况不需要考虑
-----------------------------------------------------
```

**问题12**: 如果 nums = [5]（只有一个元素），会发生什么？

```
你的回答：
-----------------------------------------------------
for循环执行一次，但找不到配对值
最终返回[]
-----------------------------------------------------
```

**问题13**: 如果 target = 10, nums = [5, 5]，会返回 [1, 1] 吗？为什么？

```
你的回答：
-----------------------------------------------------
不会返回[1, 1]
因为先存储第一个5时，下标是0
然后查找第二个5时，返回的是seen[5] = 0
最终返回[0, 1]
-----------------------------------------------------
```

**问题14**: 如果 nums 中有重复数字，比如 nums = [3, 3]，target = 6，能正确返回吗？

```
你的回答：
-----------------------------------------------------
能正确返回
第一次存储 seen[3] = 0
第二次查找 complement = 3 在 seen 中
返回 [0, 1]
-----------------------------------------------------
```

**问题15**: 如果 target 非常大（比如 10^9），Python整数会溢出吗？

```
你的回答：
-----------------------------------------------------
不会溢出
Python的int类型是任意精度的
-----------------------------------------------------
```

**问题16**: 计算复杂度

时间复杂度：
```
最坏情况下需要遍历整个数组 → O(n)
哈希表查找的时间复杂度 → O(1)
最终时间复杂度 → O(n)
```

空间复杂度：
```
最坏情况下需要存储所有元素 → O(n)
最终空间复杂度 → O(n)
```

**问题17**: 如果数组是有序的，你能想到什么更优的解法？

```
你的回答：
-----------------------------------------------------
双指针法
左指针从开头，右指针从结尾
如果和大于target，右指针左移
如果和小于target，左指针右移
-----------------------------------------------------
```

**问题18**: 有序数组场景的时间复杂度？

```
你的回答：
-----------------------------------------------------
O(n)
只需要遍历一次数组
-----------------------------------------------------
```

---

### Step 4: 费曼输出

> 目标：用自己的话复述解题思路

**问题19**: 请用自己的话（大白话）描述完整的解题思路

```
你的回答：
-----------------------------------------------------
这是一道考察哈希表查找的数组题目，时间复杂度在 O(n)。
主要思路：遍历-查找-存储，先查找，若满足条件直接输出，若不满足条件则存储。
-----------------------------------------------------
```

---

### Step 5: 举一反三

> 目标：测试模板泛化能力

**问题20**: 如果做 Hot 100 第167题（两数之和 II - 输入有序数组），基于你现在的理解，核心代码需要改哪里？

```
你的回答：
-----------------------------------------------------
（只需要描述改动点，不需要完整代码）
使用双指针
1.左指针指向开头，右指针指向结尾
2.如果和大于target，右指针左移
3.如果和小于target，左指针右移
-----------------------------------------------------
```

---

## 💾 学习笔记沉淀

### 核心模板提炼

```python
# 配对查找通用模板
def find_pair(nums, target):
    seen = {}
    for i, num in enumerate(nums):
        complement = target - num
        if complement in seen:
            return [seen[complement], i]
        seen[num] = i
    return []
```

### 记忆锚点

- **核心痛点**：如何在不重复的情况下快速找到配对元素
- **通用模式**：遍历-查找-存储
- **易错点**：遍历顺序（先查后存防止重复使用）

### 同类题目关联

- [ ] 15. 三数之和（扩展为3个数）
- [ ] 167. 两数之和 II（有序数组版本）
- [ ] 170. 两数之和 III（数据结构设计）
- [ ] 653. 两数之和 IV（BST版本）

### 复习计划

- [ ] Day 1: 完成后立即复述
- [ ] Day 2: 重新手写代码
- [ ] Day 4: 随机测试，验证默写
- [ ] Day 7: 错题回顾

---

## 🎯 今日总结

**完成状态**：□ 已完成  □ 待复习  □ 暂跳过
**掌握程度**：□ 完全掌握  □ 基本理解  □ 需要重学

**用时统计**：
- 思考阶段：____分钟
- 编码阶段：____分钟
- 调试阶段：____分钟
- 复习阶段：____分钟
- **总计**：____分钟

**收获与反思**：

```
（记录你的学习心得）

-----------------------------------------------------


-----------------------------------------------------
```

---

**下次复习日期**：______
**复习间隔**：Day 1 → Day 2 → Day 4 → Day 7 → Day 30

---

**教练签名**：Claude Code - 亚古
**记录日期**：2026-03-19
